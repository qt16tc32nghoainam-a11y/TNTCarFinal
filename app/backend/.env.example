PORT=4000
JWT_SECRET=tntcar-dev-secret-change-in-production
JWT_EXPIRES_IN=7d
DB_PATH=./data/tntcar.db
CORS_ORIGIN=http://localhost:5173

# ===== Cấu hình gửi email (SMTP) - để trống thì email chỉ ghi log =====
# Ví dụ Gmail: dùng App Password (không dùng mật khẩu thường)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=TNT CAR <no-reply@tntcar.vn>
